[pytest]
log_cli = true
log_cli_level = DEBUG
log_cli_format = "%(asctime)s %(levelname)s [%(filename)s:%(lineno)s] %(threadName)s: %(message)s"
;log_cli_date_format = %Y-%m-%d %H:%M:%S:%s

python_files = test_*
python_classes = *Tests
python_functions = test_*

;addopts = -v -s
addopts = -vv -rx